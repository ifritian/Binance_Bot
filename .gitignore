# Секреты - НИКОГДА не коммитить .env с реальными ключами
.env

# Временные файлы бота
charts/
bot.log
__pycache__/
*.pyc

# Виртуальное окружение
.venv/
venv/

# Состояние futures-бота - НЕ то же самое, что bot_state.db постинг-бота
# (тот коммитит сам GitHub Actions) - см. futures_state.py про то, почему
# это чисто локальный runtime-файл, а не история для коммитов.
futures_state.db
